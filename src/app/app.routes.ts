import { Routes } from '@angular/router';
import { MatrixViewComponent } from './matrix-view/matrix-view.component';

export const routes: Routes = [
  { title: 'matrix', path: 'matrix', component: MatrixViewComponent },
];
